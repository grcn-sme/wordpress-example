# wordpress-example
wordpress ecommerce website example

this version should be deployed as MPA to Cloudflare Pages (NOT Cloudflare Worker, not GitHub Pages)

## Cloudflare Worker

To test it locally:

```bash
npx wrangler pages dev
```
